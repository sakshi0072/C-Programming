// Take a number(n) from user & output its cube(n*n*n).

#include <stdio.h>
int main()
{
    float a;
    printf("Enter a number: ");
    scanf("%f", &a);
    printf("cube is %f", a * a * a);
    return 0;
}